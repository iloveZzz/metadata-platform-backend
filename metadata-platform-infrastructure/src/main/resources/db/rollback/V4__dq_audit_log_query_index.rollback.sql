-- =====================================================================
-- 回滚：V4__dq_audit_log_query_index.sql
-- 切片 05（RBAC / 审计）审计查询复合索引回滚（幂等，可重复执行）。
-- =====================================================================

DROP INDEX IF EXISTS idx_dq_audit_log_action_time ON dq_audit_log;
